# Bazel configuration for better debugging and development workflow

# Test output - show all output on failure (like pytest)
test:verbose --test_output=all
test:verbose --test_arg=--gtest_print_time=1

# Default test configuration for development
test --test_output=errors
test --test_arg=--gtest_print_time=1

# Keep test logs for inspection
test --keep_going

# Color output
common --color=yes

# Fast builds by default
build --compilation_mode=fastbuild

# Better error messages
build --verbose_failures
